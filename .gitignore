# Content repo nested git dir (content/ was merged in from a separate repo)
content/.git

# Astro
site/node_modules/
site/.astro/
site/dist/
site/.env
.claude/*
package-lock.json

# Covers and images — use Git LFS or omit until you decide on image hosting
# content/reviews/**/*.jpg
# content/reviews/**/*.png
